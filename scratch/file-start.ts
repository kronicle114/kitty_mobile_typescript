/* cli shortcuts - navigation

// ====== from ./src/screens =====

// make screen directories
mkdir Detail && mkdir Home && mkdir Loading && mkdir Login && mkdir Options && mkdir PasswordReset && mkdir Register && mkdir Settings; 

// add styles, index, and screen classes
cd Home/; touch styles.ts && touch index.ts && Home.tsx; cd ..; 
cd Loading/; touch styles.ts && touch index.ts && touch Loading.tsx; cd ..; 
cd Login/;  touch styles.ts && touch index.ts && touch Login.tsx; cd ..; 
cd Options/; touch styles.ts && touch index.ts && touch Options.tsx; cd ..; 
cd PasswordReset/;  touch styles.ts && touch index.ts && PasswordReset.tsx; cd ..; 
cd Register/; touch styles.ts && touch index.ts && touch Register.tsx; cd ..; 
cd Settings/; touch styles.ts && touch index.ts && touch Settings.tsx; cd ..;


*/

/* PACKAGE STUFFS 
npm install --save-dev react-native-vector-icons
npm install --save-dev @types/react-native-vector-icons

*/

// test

/* 
npm install --save-dev enzyme enzyme-adapter-react-16 jest jest-fetch-mock ts-jest react-test-renderer; npm install --save react-native-orientation; npm install --save-dev @types/enzyme @types/enzyme-adapter-react-16 @types/jest @types/react-test-renderer @types/react-native-orientation;


*/
